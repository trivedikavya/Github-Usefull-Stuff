name: Welcome Message
on: [pull_request, issues]

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: "🎉 Wow! Thanks for opening your first issue! We will look into it soon."
          pr-message: "🚀 Awesome! Thanks for your first Pull Request. Please make sure you followed the CONTRIBUTING guide!"
